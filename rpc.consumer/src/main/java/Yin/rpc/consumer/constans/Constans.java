package Yin.rpc.consumer.constans;

/**
 * `Constans` 类充当整个应用程序范围内常量值的容器。
 * 它旨在存储不可变的静态常量，这些常量在整个应用程序中使用。
 *
 * 在这个类中定义的常量旨在减少字面值的硬编码，并通过集中定义来提高可维护性。
 */
public class Constans {
	public static final String SERVER_PATH="/netty";
}
